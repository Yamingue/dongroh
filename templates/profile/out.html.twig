{% extends "base.html.twig" %}
{% block body %}

<div class="row center">
    <div class="col-sm-9">
        <!-- Material form login -->
        <div class="card mt-3 rounded">

            <h5 class="card-header primary-color-dark white-text text-center py-4 mb-2">
                <strong>Finaliser la vente</strong>
            </h5>

            <!--Card content-->
            <div class="card-body px-lg-5 pt-0 mb-3">
                <div class="card-text">
                    <div class="alert alert-info alert-dismissible fade show" role="alert">
                        Ces informations sont exclisivement demander pour finaliser la livraisons de votre commande,
                        elles ne serons aucunement rétransmisent en dehors de cette vente
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                </div>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn blue-gradient .waves-effect btn-block rounded-pill ">Commander</button>
                {{ form_end(form) }}
            </div>

        </div>
    </div>
</div>
{% endblock %}